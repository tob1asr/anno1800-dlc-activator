[project]
name = "anno1800-dlc-activator"
version = "0.1.0"
description = "Retroactively activate one or more Anno 1800 DLCs in a savegame."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.11",
]
